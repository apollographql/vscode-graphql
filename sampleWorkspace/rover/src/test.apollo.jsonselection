foo: bar
      $.results {
        id
        name
      }
      listing: {
          title: $args.input.title
          costPerNight: $args.input.costPerNight
          numOfBeds: $args.input.numOfBeds
        }
