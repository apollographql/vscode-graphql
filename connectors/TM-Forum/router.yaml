supergraph:
  listen: 127.0.0.1:4000

headers:
  all:
    request:
      - propagate:
          matching: .*
telemetry:
  instrumentation:
    spans: 
      mode: spec_compliant

# connectors:
#   subgraphs:
#     connector:
#       $config:
#         api: ${env.API_KEY}